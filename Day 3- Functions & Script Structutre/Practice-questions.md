# 📝 Practice Questions

1. Modify `is_suspicious()` to accept a threshold value
2. Add a function that counts total suspicious IPs
3. Rewrite the script to return results instead of printing them

---

✅ This day builds the foundation for writing professional Python scripts.
